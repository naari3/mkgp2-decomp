# HANDOFF batch_promote_phase1_class1batch3

Class-1 batch 3: recipe verification COMPLETE at all 8 sites; 0 fns promoted (both parked on documented non-class-1 families). Worktree TU edits = 17-line probe-ledger comment above the TickStatusEffectsByFlag asm body only (discardable; all knowledge is in docs_notes below). Build verified back at baseline SHA OK.

```json
{
  "batch_id": "batch_promote_phase1_class1batch3",
  "sub_summary": "class-1 recipe verified at all 8 sites (7+1, all u64 family, incl. new named-u64-local + ternary sub-form for CSE'd double-site); neither fn promoted - TickStatusEffectsByFlag parked >99pct on fp-scratch tie-break (Explosion family, 8 negative probes incl. exceptions on/off), KartItem_Tick parked 0-probe on class-2 frsp precan",
  "results": [
    {
      "addr": "0x80050CF0",
      "name": "KartItem_TickStatusEffectsByFlag",
      "status": "skipped",
      "src_path": null,
      "objdiff_percent": null,
      "notes": "8 build cycles. All 7 class-1 sites byte-exact (site 1 needed ternary recipe form + named u64 local fl=bus->flags; sites 2-7 plain if/else recipe). Residue = fp-scratch register permutation, 23 instrs, content identical (98.81pct measured at iter1, integer region fixed afterwards). Reverted to asm_fn in worktree; paste-ready C in docs_notes draft.",
      "blocked_reason": "fp-scratch tie-break allocator family (ItemEffect_Explosion); 8 source-form probes negative, exceptions on/off codegen-identical"
    },
    {
      "addr": "0x8004BAAC",
      "name": "KartItem_Tick",
      "status": "skipped",
      "src_path": null,
      "objdiff_percent": null,
      "notes": "fresh precan only (0 probes per standing class-2 rule). Single class-1 site is u64 family mask 0x30ULL - recipe-applicable once class 2 solved.",
      "blocked_reason": "class-2 frsp store-forward marker in entry block (frsp copies of lfs-loaded f28/f29/f30 with both versions live) - 6-probe-negative closed class"
    }
  ],
  "configure_py": {
    "add_objects": []
  },
  "splits_txt": {
    "add_entries": []
  },
  "symbols_txt": {
    "set_scope": [],
    "rename": []
  },
  "docs_notes": [
    {
      "path": "docs/notes/cw132-class1-batch3-verification.md",
      "content": "# Class-1 recipe: batch-3 verification (2026-06-11, batch_promote_phase1_class1batch3)\n\nTarget fns: KartItem_TickStatusEffectsByFlag 0x80050CF0 (7 class-1 sites, the TU max) and KartItem_Tick 0x8004BAAC (1 site). Neither fn promoted (both parked on documented NON-class-1 hard-block families), but the class-1 recipe itself verified at ALL 8 sites - the recipe ledger is now 13/13 u64-family sites byte-exact across 3 batches.\n\n## Site-by-site recipe results (TickStatusEffectsByFlag, all u64 family per precan)\n\n| # | mask | zero half | form that matched | result |\n|---|---|---|---|---|\n| 1 | 0x0000000000200000ULL | hi | ternary `b = ((fl & M) == 0) ? 0 : 1;` over named u64 local | byte-exact (see idiom 1; plain if/else recipe did NOT coalesce here - first observed coalesce failure) |\n| 2 | 0x4000000000000000ULL | lo | if/else recipe over the same named u64 local `fl` | byte-exact (CSE-reuse of site-1 flag halves) |\n| 3 | 0x0000000000000008ULL | hi | if/else recipe, direct `bus->flags` | byte-exact |\n| 4 | 0x0000100000000000ULL | lo | same | byte-exact |\n| 5 | 0x0002000000000000ULL | lo | same | byte-exact |\n| 6 | 0x2000000000000000ULL | lo | same | byte-exact |\n| 7 | 0x1000000000000000ULL | lo | same | byte-exact |\n\nKartItem_Tick site (0x8004C26C): u64 family confirmed (and/and/xor/xor/or. on bus+0x10, mask 0x30ULL, hi zero) - recipe-applicable, NOT tested in-fn (fn parked on class 2, below).\n\n## New verified idioms\n\n1. **Named u64 local for CSE'd adjacent flag tests**: when TWO class-1 sites test the same flags load back-to-back (site 2 nested under site 1), target keeps the halves in r5:r6 reusing the dead drv/owner scratch regs. Direct `bus->flags` x2 reproduces the CSE but mis-colors the whole prologue scratch region (drv=r4/zero=r5/halves r6:r7 vs target drv=r5/zero=r4/halves r5:r6) AND the site-1 bool refuses to coalesce in if/else form (li r0,0 arm survives). Fix that lands everything: `unsigned long long fl = bus->flags;` block-local + ternary recipe form for site 1 + if/else recipe form for site 2, both over `fl`. The u64 local takes the r5:r6 pair and the entire integer region goes byte-exact.\n2. **`#pragma dont_inline on` for same-TU upstream callees**: a promoted fn placed AFTER a small matched fn it calls (ItemEffect_SelectAndDispatch_Wrap, defined earlier in file = earlier address) gets the callee auto-inlined at -O4. `#pragma dont_inline on` / `reset` around the promoted fn stops it. **`#pragma auto_inline off` does NOT stop it** (negative probe). Matched callers BEFORE the callee's definition never inline (CW only inlines already-seen bodies) - that is why this never bit earlier promotes.\n3. **Volatile-cast const reload in a speculated else arm** (batch-1 ShadowBB idiom, 2nd confirmation): `TornadoEffect_TriggerWheelScaleAnim(ow, *(volatile const float *)&lbl_806D2700);` in the else arm forces the fresh lfs where CW would CSE with the earlier fcmpo load and bridge with fmr.\n4. **Param-register-reuse via variable reassignment** (itemeffect idiom 1 transfer): `ow` holds effectObj (r30) for the Tornado calls, then `ow = self->owner;` in the 0x0002... block reuses r30 exactly like target.\n\n## fn 1 park: fp-scratch tie-break (ItemEffect_Explosion family, new data point)\n\nFinal residue: 23 instructions in the 0x0000100000000000 (color-pulse) block, instruction content 100% identical, fp scratch register PERMUTATION only. Target numbers the 3 const/temp web clusters bottom-up (later-first-use -> lower reg: one=f0/inc=f1/old=f2; C=f0/one2=f1; c0=f0/c1=f1 + arg4-fmadds emitted before arg3-fmadds); CW emits the top-down permutation. 8 negative probes: named const hoists (copy-propagated), volatile-cast load, assignment-in-expression `(inv = 1.0f - s)`, addend-first fmadds spelling, compound +=, fn-top vs block decl scoping, static inline wrap helper, prototyped vs K&R float callees (byte-neutral both ways), `#pragma exceptions off` removed (codegen IDENTICAL -> NOT the approach-B regalloc class). Strongest new observation for the family ledger: the IDENTICAL source shape (`a = a + C; if (a > C2) a = a - C2;`) produced target-style bottom-up numbering at the 0x0002... site (matched) but top-down at the 0x0000100... site - the flip correlates with the only structural difference, a multi-block const web (the wrap constant reused by the conditional fsubs arm) coexisting with downstream same-block fp code. The ordering is allocator-internal; no source lever found.\n\nPaste-ready C (>99% by content, fp renames only) saved at docs/drafts/KartItem_TickStatusEffectsByFlag_99pct.handoff.md. Prereqs for paste: retype externs lbl_806D27B4/B8/BC/C0/C4 + lbl_806D2768 -> const float; fn_802DCA04 -> float(void); fn_8027E9E8 -> double(double); ShadowBillboard_SetField0xA4 / TornadoEffect_SetColorRGBLerp prototypes optional (byte-neutral).\n\n## fn 2 park: KartItem_Tick 0x8004BAAC precan (0 probes, classes already closed)\n\n- **class 2 frsp store-forward**: entry block loads f30/f29/f28 = movement +0x17c/0x180/0x184 (velocity vec, callee-saved, also dead-stored to sp+0x20..0x28) AND makes frsp scratch copies `frsp f0,f29 / frsp f1,f30 / frsp f2,f28` for the magnitude sum - frsp of lfs-loaded singles with both versions live = the 6-probe-negative ItemEffect_Dispatch shape. Park per standing rule.\n- The big sp+0x2c..0x68 block is the 4x4 identity init + view-matrix copy with double-overwrite stores (volatile scratch struct family, solvable) and the two other frsp (in-place MSL sqrtf NR at +0x1ac; `(float)__fabs` at L_8004C128) are reachable idioms - the fn is ~95 percent reachable EXCEPT the frsp trio.\n- No ctr loops, no `bl __unexpected`, no FP-prologue, no ScopedTimer subi 0x217d, no symbol+const table walk. 1 class-1 site (u64, mask 0x30ULL).\n- Unlock path: solving class 2 self-corrects this fn; recommend bundling with ItemEffect_Dispatch (86 percent draft) in any future class-2 research batch.\n"
    },
    {
      "path": "docs/drafts/KartItem_TickStatusEffectsByFlag_99pct.handoff.md",
      "content": "# KartItem_TickStatusEffectsByFlag paste-ready C (batch_promote_phase1_class1batch3, 2026-06-11)\n\nContent-identical to target except the fp-scratch register permutation in the 0x0000100000000000 block (23 instrs; see docs/notes/cw132-class1-batch3-verification.md). All 7 class-1 sites + all integer code byte-exact.\n\nExtern prereqs: lbl_806D27B4/27B8/27BC/27C0/27C4/2768 -> `extern const float`; `extern float fn_802DCA04(void);`; `extern double fn_8027E9E8(double);`. View structs (ItemEffectTable / ItemBusFlagsView / KartItemOpsView / KartDriverBusView) already in the TU.\n\n```c\n#pragma exceptions off\n#pragma dont_inline on\nvoid KartItem_TickStatusEffectsByFlag(ItemEffectTable *self) { /* 0x80050CF0 size:0x410 */\n    ItemBusFlagsView *bus;   /* r31 */\n    KartItemOpsView *ow;     /* r30: effectObj first, owner later (param-reuse) */\n    unsigned char ok;        /* r28 */\n    KartItemOpsView *o;      /* scratch */\n\n    o = self->owner;\n    bus = o->ownerDriver->itemBus;\n    ow = o->effectObj;\n\n    {\n        unsigned long long fl;\n        unsigned char b;\n        fl = bus->flags;\n        b = ((fl & 0x0000000000200000ULL) == 0) ? 0 : 1;\n        if (b == 1) {\n            unsigned char c;\n            if ((fl & 0x4000000000000000ULL) == 0) {\n                c = 0;\n            } else {\n                c = 1;\n            }\n            if (c == 1) {\n                if (ItemEffect_SelectAndDispatch_Wrap(self, 0x118, 0, -1) == 0) {\n                    TornadoEffect_ApplyItemVisual_Secondary(ow, 0x118, 1);\n                }\n            }\n        }\n    }\n    {\n        unsigned char b;\n        if ((bus->flags & 0x0000000000000008ULL) == 0) {\n            b = 0;\n        } else {\n            b = 1;\n        }\n        if (b == 1) {\n            ok = 1;\n            self->run1c = self->run1c + 1;\n            if (self->run1c > 0x14) {\n                self->run1c = 0;\n                if (fn_802DCA04() < lbl_806D2700) {\n                    ok = 0;\n                }\n                if (ok == 1) {\n                    TornadoEffect_TriggerWheelScaleAnim(ow, lbl_806D2708);\n                    EffectSteering_InputViscosity_SetFieldC(self->effectState, lbl_806D26FC);\n                } else {\n                    TornadoEffect_TriggerWheelScaleAnim(ow, *(volatile const float *)&lbl_806D2700);\n                    EffectSteering_InputViscosity_SetFieldC(self->effectState, lbl_806D26EC);\n                }\n            }\n        }\n    }\n    {\n        unsigned char b;\n        if ((bus->flags & 0x0000100000000000ULL) == 0) {\n            b = 0;\n        } else {\n            b = 1;\n        }\n        if (b == 1) {\n            float s;                 /* f31 */\n            float inv;               /* f30 */\n            float colA;\n            float colB;\n\n            self->runBlend20 += lbl_806D27B4;\n            if (self->runBlend20 > lbl_806D26FC) {\n                self->runBlend20 -= lbl_806D26FC;\n            }\n            s = (float)fn_8027E9E8(lbl_806D27B8 * self->runBlend20);\n            inv = lbl_806D26FC - s;\n            ShadowBillboard_SetField0xA4(self->owner->billboard, lbl_806D27BC - lbl_806D27BC * inv);\n            colB = lbl_806D26E8 + lbl_806D2768 * inv;\n            colA = lbl_806D26E8 + lbl_806D2768 * s;\n            TornadoEffect_SetColorRGBLerp(ow, 1, colA, colB, lbl_806D26FC);\n        }\n    }\n    {\n        unsigned char b;\n        if ((bus->flags & 0x0002000000000000ULL) == 0) {\n            b = 0;\n        } else {\n            b = 1;\n        }\n        if (b == 1) {\n            self->runBlend24 = self->runBlend24 + lbl_806D2730;\n            if (self->runBlend24 > lbl_806D2700) {\n                unsigned char done;\n\n                ow = self->owner;\n                if (fn_8019A8A4(ow->movement, lbl_806D26FC) == 0) {\n                    done = 0;\n                } else {\n                    void *stats;\n\n                    if ((stats = lbl_806D10A0) == 0) {\n                        stats = 0;\n                    }\n                    if (stats != 0) {\n                        TitleStats_IncTotalRaces(stats, ow->raceScore10);\n                    }\n                    SoundObj_PlaySE_Direct(ow->soundCtrl, 0x56);\n                    done = 1;\n                }\n                if (done == 1) {\n                    self->runBlend24 = lbl_806D26EC;\n                }\n            }\n        }\n    }\n    {\n        unsigned char b;\n        if ((bus->flags & 0x2000000000000000ULL) == 0) {\n            b = 0;\n        } else {\n            b = 1;\n        }\n        if (b == 1) {\n            if (EffectState_HasContent(self->effectState) == 0) {\n                self->intensity10 = self->intensity10 * lbl_806D26E4;\n                EffectSteering_InitForVibrate(self->effectState, lbl_806D26FC, lbl_806D27C0 * self->intensity10, lbl_806D27C4);\n            }\n        }\n    }\n    {\n        unsigned char b;\n        if ((bus->flags & 0x1000000000000000ULL) == 0) {\n            b = 0;\n        } else {\n            b = 1;\n        }\n        if (b == 1) {\n            if (self->laneKindC == 1) {\n                void *cam;\n\n                if ((cam = (void *)lbl_806D109C) == 0) {\n                    cam = 0;\n                }\n                CameraEffect_Apply(cam, 1, lbl_806D26FC);\n            }\n        }\n    }\n}\n#pragma dont_inline reset\n#pragma exceptions reset\n```\n"
    }
  ],
  "build_verified": {
    "command": "ninja build/GNLJ82/ok",
    "sha1_ok": true,
    "funcs_matched_delta": 0
  },
  "blocked_reason": null,
  "user_attention": null
}
```
