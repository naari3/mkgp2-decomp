# approach-B #pragma exceptions off vs CW 1.3.2 late-web register pick (2026-06-10, CarObject_HandleObstacleHit 0x8004AE28)

In extab-bearing bundles promoted with approach B (manual extab kept + C body in #pragma exceptions off/reset), CW 1.3.2 allocates a LATE fresh callee-saved web (created after all other webs died) to the highest free / most-recently-expired register (observed r31 = the cat web reg, robust across ~8 source forms: fresh var any decl position/scope, same-variable reuse incl casts, register kw, ptr-arith folds, static inline helper). The original binary (compiled -Cpp_exceptions on) assigned r24 (the reg of an earlier-dead web). With exceptions on the pick moves (r22 observed) but exceptions on cannot be used when the fn sits behind asm_fn manual-extab entries in the TU extab layout (auto-emit links before .extab_user, breaking extab order -> SHA-1 fail; see per_fn_matching_strategy 14.1 mix rule). Symptom: 100% instruction/shape match except N instructions differing in ONE register on the last-allocated pointer web. If you see this under approach B, check whether the fn has a real extab (compiled exceptions-on originally) before burning cycles on source permutations. CarObject_HandleObstacleHit reached 99.76% (9 insns off); full working-source notes in batch_promote_8004ae28_obstaclehit HANDOFF (preserved in git log of commit recording this note).
